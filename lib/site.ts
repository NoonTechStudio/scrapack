// Central site config — used by metadata, sitemap, robots and analytics.

/**
 * Canonical production origin (no trailing slash).
 * Must match the domain the site actually serves: Vercel currently
 * 308-redirects the apex to `www`, so `www` is canonical. If you make the
 * apex primary in Vercel instead, drop the `www.` here.
 */
export const SITE_URL = "https://www.scrapacksolutions.com";

export const SITE_NAME = "ScraPack Solutions LLC";
